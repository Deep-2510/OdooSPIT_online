import React from 'react';

const DocumentWorkflow = ({ children }) => (
  <div className="document-workflow">
    <h4>Document Workflow</h4>
    {children}
  </div>
);

export default DocumentWorkflow;
